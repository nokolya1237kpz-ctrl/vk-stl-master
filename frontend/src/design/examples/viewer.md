# Viewer Example

Источник: `StlPreview`, `previewCanvas`, `studioViewportV8`, `stlMarketingViewer`, `compareCanvas`.

## После восстановления

Модель остаётся главным объектом, overlays не доминируют, section plane/gizmo/HUD не перекрывают важные части.

## Ограничение

Three.js scene logic, camera, STLLoader and processing data не меняются.
