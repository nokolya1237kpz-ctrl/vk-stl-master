# Section Example

Источник: `SectionHeader`, `launchSectionHeader`, `sectionNumber`, `sectionLabel`, `publicSection`.

## После восстановления

Все публичные секции используют единый kicker, title, optional text/action и spacing из Stage 02 scale.

## Ограничение

DOM и порядок секций не меняются.
