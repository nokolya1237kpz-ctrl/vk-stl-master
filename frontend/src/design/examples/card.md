# Card Example

Источник: `heroBenefits`, `connectionCard`, `historyCard`, `premiumTipCard`, `adminSummaryGrid`.

## После восстановления

Карточка имеет один radius, читаемый header, стабильный media/content area и предсказуемый hover только если карточка интерактивна.

## Ограничение

Нельзя менять данные, порядок карточек, ссылки, API-состояния или тексты без отдельного задания.
